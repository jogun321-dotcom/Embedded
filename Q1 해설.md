## 📌 핀 설정

```cpp
const int sw1 = 2;
const int sw2 = 3;
const int sw3 = 4;
```
스위치 3개를 디지털 핀 2, 3, 4번에 연결.

```cpp
const int ledR = 5;
const int ledG = 6;
const int ledB = 7;
```
RGB LED의 R/G/B 각각을 핀 5, 6, 7번에 연결.

```cpp
const int fndPins[4] = {8, 9, 10, 11};
```
7세그먼트(FND)에 숫자를 보내는 BCD 신호 핀 4개 (A, B, C, D). 74LS47 디코더 IC를 거쳐 FND에 전달됨.

```cpp
const int digit1 = 12; // 십의 자리
const int digit2 = 13; // 일의 자리
```
FND가 2자리이므로, 어느 자리를 켤지 선택하는 핀.

---

## 📌 상태 변수

```cpp
int currentState = 0;
```
현재 동작 상태. `0`=초기화 시퀀스, `-1`=초기화 완료 대기, `1`=동작1, `2`=동작2.

```cpp
int currentNumber = 0;
```
FND에 표시할 현재 숫자 (0~99).

```cpp
bool lastSw1 = HIGH;
bool lastSw2 = HIGH;
```
SW1, SW2의 이전 상태 저장. "눌렀다 뗄 때"를 감지(엣지 감지)하기 위해 필요.

---

## 📌 `setup()`

```cpp
pinMode(sw1~sw3, INPUT_PULLUP);
```
스위치 핀을 내부 풀업 저항 입력으로 설정. 스위치 안 눌리면 HIGH, 눌리면 LOW.

```cpp
pinMode(ledR/G/B, OUTPUT);
```
RGB LED 핀 출력 설정.

```cpp
for (int i = 0; i < 4; i++) pinMode(fndPins[i], OUTPUT);
```
BCD 핀 4개 출력 설정.

```cpp
pinMode(digit1, OUTPUT);
pinMode(digit2, OUTPUT);
```
FND 자리 선택 핀 출력 설정.

---

## 📌 `loop()`

```cpp
if (currentState == 0) runInit();
```
상태가 0이면 초기화 시퀀스 실행.

```cpp
else if (currentState == -1) { ... }
```
초기화 완료 후 대기 상태. `00`을 표시하며 스위치 입력 기다림.

```cpp
if (ev == 3) currentState = 0;   // SW3 → 초기화로
else if (ev == 1) currentState = 1; // SW1 → 동작1로
else if (ev == 2) currentState = 2; // SW2 → 동작2로
```
대기 중 어떤 스위치를 누르냐에 따라 다음 상태로 전환.

---

## 📌 `setRGB(r, g, b)`

```cpp
void setRGB(int r, int g, int b) {
  digitalWrite(ledR, r);
  digitalWrite(ledG, g);
  digitalWrite(ledB, b);
}
```
RGB LED를 한 번에 제어하는 편의 함수. 예: `setRGB(HIGH, LOW, LOW)` → 빨간색.

---

## 📌 `waitForEvent()` ← 핵심 함수

```cpp
int waitForEvent(int num, unsigned long durationMs, bool ignoreInput)
```
지정된 시간(`durationMs`) 동안 FND에 숫자를 표시하면서 스위치 입력을 감지. 반환값: 눌린 스위치 번호(1/2/3), 없으면 0.

```cpp
unsigned long start = millis();
while (millis() - start < durationMs) {
```
현재 시간 기록 후, 경과 시간이 `durationMs`를 넘을 때까지 반복.

```cpp
int tens = num / 10;
int ones = num % 10;
```
숫자를 십의 자리/일의 자리로 분리.

```cpp
for (int i = 0; i < 4; i++) digitalWrite(fndPins[i], (tens >> i) & 1);
digitalWrite(digit1, HIGH); digitalWrite(digit2, LOW);
delay(2);
```
십의 자리 BCD 신호 출력 → digit1만 켬 → 2ms 유지.

```cpp
for (int i = 0; i < 4; i++) digitalWrite(fndPins[i], (ones >> i) & 1);
digitalWrite(digit1, LOW); digitalWrite(digit2, HIGH);
delay(2);
```
일의 자리 BCD 신호 출력 → digit2만 켬 → 2ms 유지. (멀티플렉싱: 두 자리를 빠르게 번갈아 켜서 동시에 켜진 것처럼 보이게 함)

```cpp
digitalWrite(digit1, LOW); digitalWrite(digit2, LOW);
```
잔상 방지를 위해 두 자리 모두 끔.

```cpp
if (num < 0) { digitalWrite(digit1, LOW); digitalWrite(digit2, LOW); delay(4); }
```
`num`이 음수(-1)면 FND 완전 소등.

```cpp
if (digitalRead(sw3) == LOW) return 3;
```
SW3는 누르는 즉시 감지(레벨 감지).

```cpp
bool currSw1 = digitalRead(sw1);
if (lastSw1 == LOW && currSw1 == HIGH) { lastSw1 = currSw1; return 1; }
lastSw1 = currSw1;
```
SW1은 LOW→HIGH로 바뀌는 순간(뗄 때)만 감지(엣지 감지). SW2도 동일한 방식.

```cpp
return 0;
```
시간 내 스위치 입력 없으면 0 반환.

---

## 📌 `runInit()` — 초기화 시퀀스

```cpp
setRGB(HIGH, LOW, LOW); waitForEvent(88, 1000, true);
```
빨간 LED 켜고, FND에 `88` 표시하며 1초 대기. (스위치 무시)

```cpp
setRGB(LOW, HIGH, LOW); waitForEvent(-1, 1000, true);
```
초록 LED 켜고, FND 끄고 1초 대기.

```cpp
setRGB(LOW, LOW, HIGH); waitForEvent(88, 1000, true);
```
파란 LED 켜고, FND `88` 표시 1초 대기.

```cpp
setRGB(LOW, LOW, LOW); waitForEvent(-1, 1000, true);
```
LED·FND 모두 끄고 1초 대기.

```cpp
currentState = -1;
setRGB(HIGH, LOW, LOW);
```
초기화 완료 → 대기 상태로 전환. 빨간 LED 점등 유지.

---

## 📌 `runAction1()` — 동작 1 (숫자 증가)

```cpp
setRGB(LOW, HIGH, LOW); waitForEvent(88, 500, false);
setRGB(HIGH, LOW, LOW); waitForEvent(88, 500, false);
setRGB(LOW, LOW, LOW);  waitForEvent(88, 1000, false);
```
진입 연출: 초록 0.5초 → 빨강 0.5초 → 소등 1초, FND는 `88` 유지. 이 때 스위치 입력도 감지.

```cpp
currentNumber = 0;
while (true) {
  setRGB(LOW, LOW, HIGH);
  handleAction1Event(waitForEvent(currentNumber, 500, false), currentNumber);
  currentNumber = (currentNumber + 1) % 100;
  setRGB(LOW, LOW, LOW);
  handleAction1Event(waitForEvent(currentNumber, 500, false), currentNumber);
  currentNumber = (currentNumber + 1) % 100;
}
```
0부터 시작해 0.5초마다 1씩 증가. 파란 LED 켬/끔을 번갈아 가며 숫자 표시. 99 다음은 다시 00으로 순환.

---

## 📌 `handleAction1Event()` — 동작 1 이벤트 처리

```cpp
if (ev == 3) { currentState = 0; return true; }
```
SW3 → 초기화로 이동 (루프 탈출).

```cpp
if (ev == 2) { currentState = 2; return true; }
```
SW2 → 동작2로 즉시 전환 (루프 탈출).

```cpp
if (ev == 1) { runAction3(num); return false; }
```
SW1 → 일시정지(동작3) 수행 후 동작1 계속 진행 (루프 유지).

---

## 📌 `runAction2()` — 동작 2 (숫자 감소)

동작1과 구조 동일하지만 방향이 반대.

```cpp
currentNumber = 99;
currentNumber = (currentNumber == 0) ? 99 : currentNumber - 1;
```
99부터 시작해 0.5초마다 1씩 감소. 0 다음은 다시 99로 순환. LED 색은 초록 켬/끔.

진입 연출은 파란 0.5초 → 빨강 0.5초 → 소등 1초.

---

## 📌 `handleAction2Event()` — 동작 2 이벤트 처리

```cpp
if (ev == 3) { currentState = 0; return true; } // SW3 → 초기화
if (ev == 1) { currentState = 1; return true; } // SW1 → 동작1로 전환
if (ev == 2) { runAction3(num); return false; } // SW2 → 일시정지 후 계속
```
동작1과 SW1/SW2 역할이 서로 바뀐 것이 차이점.

---

## 📌 `runAction3()` — 동작 3 (일시정지)

```cpp
for (int i = 0; i < 2; i++) {
  setRGB(HIGH, LOW, LOW); waitForEvent(num, 2000, true);
  setRGB(LOW, LOW, LOW);  waitForEvent(-1, 2000, true);
}
```
현재 숫자를 고정한 채로, 빨간 LED 2초 켬 → FND·LED 소등 2초, 이것을 2번 반복. 스위치 입력 완전 무시. 반복 후 호출한 동작(1 또는 2)으로 자동 복귀.

---

**전체 흐름 요약:**

```
전원ON → 초기화 시퀀스(R→G→B 순차) → 대기(00 표시)
  ├── SW1 → 동작1: 00~99 증가 (파란 LED 점멸)
  │     ├── SW1 → 일시정지(동작3) → 복귀
  │     ├── SW2 → 동작2로 전환
  │     └── SW3 → 초기화로
  └── SW2 → 동작2: 99~00 감소 (초록 LED 점멸)
        ├── SW2 → 일시정지(동작3) → 복귀
        ├── SW1 → 동작1로 전환
        └── SW3 → 초기화로
```
