## 📌 핀 설정

```cpp
const int sw1 = 2;
const int sw2 = 3;
const int sw3 = 4;
```
스위치 3개를 디지털 핀 2, 3, 4번에 연결.

```cpp
const int ledR = 5;
const int ledG = 6;
const int ledB = 7;
```
RGB LED의 R/G/B를 핀 5, 6, 7번에 연결.

```cpp
const int fndPins[4] = {8, 9, 10, 11};
```
FND에 숫자를 전달하는 BCD 신호 핀 4개 (74LS47 IC 경유).

```cpp
const int digit1 = 12; // 십의 자리
const int digit2 = 13; // 일의 자리
```
2자리 FND에서 어느 자리를 켤지 선택하는 핀.

---

## 📌 상태 변수

```cpp
int currentState = 0;
```
현재 동작 상태. `0`=초기화 중, `-1`=대기, `1`=동작1, `2`=동작2, `3`=동작3.
이전 코드와 달리 **동작3이 독립적인 상태(3)** 로 분리됨.

```cpp
int currentNumber = 50;
```
FND에 표시할 숫자. 초기값이 이전 코드(0)와 달리 **50**으로 시작.

```cpp
unsigned long sw1PressTime = 0;
unsigned long sw2PressTime = 0;
```
SW1/SW2가 처음 눌린 시각 저장. **길게 누름(1초) 감지**를 위해 사용. 이전 코드에는 없던 기능.

---

## 📌 `setup()`

```cpp
pinMode(sw1~sw3, INPUT_PULLUP);
```
스위치 핀을 내부 풀업 입력으로 설정. 안 누르면 HIGH, 누르면 LOW.

```cpp
pinMode(ledR/G/B, OUTPUT);
```
RGB LED 핀 출력 설정.

```cpp
for (int i = 0; i < 4; i++) pinMode(fndPins[i], OUTPUT);
pinMode(digit1, OUTPUT);
pinMode(digit2, OUTPUT);
```
BCD 핀 4개 + 자리 선택 핀 2개 출력 설정.

---

## 📌 `loop()`

```cpp
if (currentState == 0) runInit();
else if (currentState == -1) runIdle();
else if (currentState == 1) runAction1();
else if (currentState == 2) runAction2();
else if (currentState == 3) runAction3();
```
상태값에 따라 해당 함수 호출. 이전 코드는 `loop()` 안에서 직접 대기 처리를 했지만, 이번엔 **`runIdle()` 함수로 분리**됨.

---

## 📌 `refreshFND(num)` — FND 갱신

```cpp
void refreshFND(int num) {
```
약 4ms 동안 FND 두 자리를 한 번씩 켜주는 함수. 이전 코드의 `waitForEvent()` 내부에 있던 멀티플렉싱 로직을 **독립 함수로 분리**함.

```cpp
if (num < 0) {
  digitalWrite(digit1, LOW);
  digitalWrite(digit2, LOW);
  delayMicroseconds(4000);
  return;
}
```
num이 음수면 FND 완전 소등 후 4ms 대기하고 종료.

```cpp
int tens = num / 10;
int ones = num % 10;
```
숫자를 십의 자리 / 일의 자리로 분리.

```cpp
for(int i=0; i<4; i++) digitalWrite(fndPins[i], (tens >> i) & 1);
digitalWrite(digit1, HIGH); digitalWrite(digit2, LOW);
delayMicroseconds(2000);
```
십의 자리 BCD 신호 출력 → digit1만 켬 → 2ms 유지.
이전 코드는 `delay(2)`(2ms)를 썼는데, 이번엔 `delayMicroseconds(2000)`으로 **더 정밀하게** 처리.

```cpp
for(int i=0; i<4; i++) digitalWrite(fndPins[i], (ones >> i) & 1);
digitalWrite(digit1, LOW); digitalWrite(digit2, HIGH);
delayMicroseconds(2000);
```
일의 자리 BCD 신호 출력 → digit2만 켬 → 2ms 유지.

```cpp
digitalWrite(digit1, LOW); digitalWrite(digit2, LOW);
```
잔상 방지를 위해 두 자리 모두 끔.

---

## 📌 `setRGB(r, g, b)`

```cpp
void setRGB(int r, int g, int b) {
  digitalWrite(ledR, r);
  digitalWrite(ledG, g);
  digitalWrite(ledB, b);
}
```
RGB LED 색상을 한 번에 설정하는 편의 함수. 이전 코드와 동일.

---

## 📌 `waitWithFND(num, ms, r, g, b)` — 대기 함수

```cpp
void waitWithFND(int num, unsigned long ms, int r, int g, int b) {
```
지정된 시간(ms) 동안 특정 숫자와 LED 색상을 **유지**하는 함수. 이전 코드의 `waitForEvent()`와 비슷하지만 **스위치 감지 없음**, 대신 **LED 색상을 인자로 받음**.

```cpp
setRGB(r, g, b);
```
LED 색상 먼저 설정.

```cpp
unsigned long start = millis();
while(millis() - start < ms) {
  refreshFND(num);
}
```
경과 시간이 ms를 넘을 때까지 `refreshFND()`를 계속 호출해 FND 표시 유지.

---

## 📌 `runInit()` — 초기화 (상태 0)

```cpp
waitWithFND(88, 1000, LOW, HIGH, LOW); // FND 88, 녹색 1초
waitWithFND(-1, 1000, LOW, LOW, HIGH); // FND 소등, 청색 1초
waitWithFND(88, 1000, LOW, HIGH, LOW); // FND 88, 녹색 1초
waitWithFND(-1, 1000, LOW, LOW, HIGH); // FND 소등, 청색 1초
```
녹색↔청색을 1초씩 번갈아 2회 반복. FND는 88과 소등을 교대로 표시.
이전 코드는 R→G→B→소등 4단계였지만, 이번엔 **G↔B 2회 반복**으로 변경됨.

```cpp
currentNumber = 50;
setRGB(HIGH, LOW, LOW);
currentState = -1;
```
초기화 완료. 숫자를 50으로 설정, 빨간 LED 켬, 대기 상태로 전환.

---

## 📌 `runIdle()` — 대기 상태 (상태 -1)

이전 코드는 `loop()` 안에서 처리했지만, 이번엔 **전용 함수로 분리**됨.

```cpp
refreshFND(currentNumber);
```
매 호출마다 FND에 현재 숫자 표시 유지 (처음엔 50).

```cpp
if (digitalRead(sw3) == LOW) {
  currentState = 0;
  sw1PressTime = 0; sw2PressTime = 0;
  return;
}
```
SW3 누르면 즉시 초기화. 타이머 변수도 함께 초기화.

```cpp
if (digitalRead(sw1) == LOW) {
  if (sw1PressTime == 0) sw1PressTime = millis();
  else if (millis() - sw1PressTime >= 1000) {
    currentState = 1;
    sw1PressTime = 0;
  }
} else {
  sw1PressTime = 0;
}
```
SW1 길게 누름 감지:
- 처음 눌린 순간 시각 기록.
- 1초 이상 유지되면 동작1로 전환.
- 중간에 떼면 타이머 초기화 (짧게 눌러도 전환 안 됨).

이전 코드는 짧게 눌렀다 떼면 전환됐지만, 이번엔 **1초 이상 길게 눌러야** 전환.

```cpp
if (digitalRead(sw2) == LOW) {
  if (sw2PressTime == 0) sw2PressTime = millis();
  else if (millis() - sw2PressTime >= 1000) {
    currentState = 2;
    sw2PressTime = 0;
  }
} else {
  sw2PressTime = 0;
}
```
SW2도 동일하게 1초 길게 누름 시 동작2로 전환.

---

## 📌 `runAction1()` — 동작 1 (숫자 감소)

```cpp
unsigned long lastTick = millis();
bool ledOn = true;
setRGB(LOW, LOW, HIGH);
```
0.5초 타이머 초기화, 청색 LED 켬으로 시작.

```cpp
unsigned long zeroReachTime = 0;
```
숫자가 00에 도달한 시각 저장용 변수.

```cpp
while(true) {
  refreshFND(currentNumber);
```
무한루프 안에서 계속 FND 갱신.

```cpp
if (digitalRead(sw1) == HIGH) {
  setRGB(HIGH, LOW, LOW);
  currentState = -1;
  return;
}
```
**SW1을 떼는 순간** 동작 종료 → 빨간 LED → 대기 상태로.
이전 코드와 달리 **누르고 있는 동안만 동작**, 떼면 즉시 멈춤.

```cpp
if (currentNumber == 0) {
  if (zeroReachTime == 0) zeroReachTime = millis();
  else if (millis() - zeroReachTime >= 1000) {
    currentState = 3;
    return;
  }
}
```
숫자가 00에 도달하면 타이머 시작, 1초 더 유지되면 동작3으로 전환.

```cpp
if (millis() - lastTick >= 500) {
  lastTick = millis();
  if (currentNumber > 0) {
    currentNumber--;
    zeroReachTime = 0;
  }
  ledOn = !ledOn;
  if (ledOn) setRGB(LOW, LOW, HIGH);
  else setRGB(LOW, LOW, LOW);
}
```
0.5초마다: 숫자 1 감소 (0 이하로는 안 내려감) + 00 도달 전이면 타이머 리셋 + 청색 LED 점멸 전환.

---

## 📌 `runAction2()` — 동작 2 (숫자 증가)

동작1과 구조 동일, 방향 반대.

```cpp
setRGB(LOW, HIGH, LOW);
```
녹색 LED로 시작.

```cpp
if (digitalRead(sw2) == HIGH) {
  setRGB(HIGH, LOW, LOW);
  currentState = -1;
  return;
}
```
SW2 떼는 순간 종료 → 대기 상태로.

```cpp
if (currentNumber == 99) {
  if (ninetyNineReachTime == 0) ninetyNineReachTime = millis();
  else if (millis() - ninetyNineReachTime >= 1000) {
    currentState = 3;
    return;
  }
}
```
숫자가 99에 도달 후 1초 유지되면 동작3으로 전환.

```cpp
if (currentNumber < 99) {
  currentNumber++;
  ninetyNineReachTime = 0;
}
ledOn = !ledOn;
if (ledOn) setRGB(LOW, HIGH, LOW);
else setRGB(LOW, LOW, LOW);
```
0.5초마다 숫자 1 증가 (99 초과 안 함) + 녹색 LED 점멸.

---

## 📌 `runAction3()` — 동작 3 (상태 3)

이전 코드는 동작1/2 중 일시정지 서브루틴이었지만, 이번엔 **독립적인 상태**로 분리됨.

```cpp
waitWithFND(0,  1000, HIGH, LOW, LOW); // 00 표시, R(적) 1초
waitWithFND(99, 1000, LOW, HIGH, LOW); // 99 표시, G(녹) 1초
waitWithFND(0,  1000, LOW, LOW, HIGH); // 00 표시, B(청) 1초
waitWithFND(99, 1000, LOW, LOW, LOW);  // 99 표시, 소등 1초
```
R→G→B→소등 순으로 각 1초씩, FND는 0↔99를 교대 표시. 스위치 입력 완전 무시.

```cpp
currentNumber = 50;
setRGB(HIGH, LOW, LOW);
currentState = -1;
```
시퀀스 완료 후 숫자 50으로 리셋, 빨간 LED, 대기 상태로 복귀.
이전 코드는 호출한 동작(1 또는 2)으로 돌아갔지만, 이번엔 **무조건 대기 상태로 복귀**.

---

**이전 코드와의 핵심 차이점 요약:**

| 항목 | 이전 코드 | 이번 코드 |
|------|----------|----------|
| 초기 숫자 | 0 | **50** |
| 스위치 방식 | 짧게 눌렀다 뗌 | **1초 길게 누름** |
| 동작 유지 방식 | 누르면 전환 후 유지 | **누르는 동안만 동작** |
| 동작3 위치 | 서브루틴 (동작1/2 내부) | **독립 상태(3)** |
| 동작3 이후 복귀 | 동작1 또는 2로 복귀 | **항상 대기 상태로** |
| 초기화 연출 | R→G→B→소등 | **G↔B 2회 반복** |
| 대기 처리 | loop() 직접 처리 | **runIdle() 분리** |
